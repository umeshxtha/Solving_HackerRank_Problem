function padRow() {
    const call = "padRow";
    return call;
}

const call = padRow(); // Assigning the result of the padRow call to a variable named 'call'

console.log(call); // Output: "padRow"
